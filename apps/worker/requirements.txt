fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.27.2
pytest==8.3.3

# Deterministic PDF extraction (Sprint 1.2B) — see ADR 0010.
pypdf==6.14.2

# Fixture generation only (apps/worker/tests/fixtures/pdf/generate_fixtures.py).
# Never imported by app/pdf_extraction/* or any test's assertions — the
# generated .pdf files are committed as static binary fixtures, so pytest
# itself does not need these at run time. Kept in this single
# requirements.txt (matching this project's one-file convention) rather
# than a separate requirements-dev.txt, since they're only needed if a
# fixture is ever regenerated.
reportlab==4.2.5
pillow==12.3.0

# Controlled page-scoped OCR (Sprint 1-D2) — see ADR 0012.
# pypdfium2 renders the exact registered PDF page to a pixel image
# (app/ocr/renderer.py); pinned version must match app/ocr/config.py's
# RENDERER_VERSION, enforced at startup by assert_pinned_renderer_version().
pypdfium2==5.12.1
# pytesseract is a thin subprocess wrapper around the system `tesseract`
# binary (app/ocr/provider.py) — the binary itself is installed by the
# Dockerfile (apt package tesseract-ocr), not by this requirements file;
# pinned version must match app/ocr/config.py's OCR_PROVIDER_VERSION,
# enforced at startup by assert_pinned_provider_version().
pytesseract==0.3.13
